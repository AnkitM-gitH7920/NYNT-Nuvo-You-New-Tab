import "./settings-window.css";
import { X } from "lucide-react";

export default function SettingsWindow({ closeSettings }) {
     return (
          <div className="settings-window">
                    <div className="sw-head">
                         Settings
                    </div>
          </div>
     )
}
